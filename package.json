{
  "name": "eden-pods-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm run build",
    "install:web": "cd apps/web && npm install",
    "setup": "cd apps/web && npm install && echo 'Done. Run: npm run dev'"
  }
}
